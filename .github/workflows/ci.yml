name: CI

"on":
  push:
    branches: [master, main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install test dependencies
        run: python -m pip install -e ".[test]"
      - name: Run tests
        run: python -m pytest -q
      - name: Check shell launchers
        run: |
          test -x script/verl/common/create_env.sh
          test -x script/verl/common/install_flash_attn.sh
          test -x script/verl/sft/run.sh
          test -x script/verl/sft/smoke.sh
          bash -n script/verl/common/create_env.sh
          bash -n script/verl/common/install_flash_attn.sh
          bash -n script/verl/sft/run.sh
          bash -n script/verl/sft/smoke.sh
      - name: Compile Python sources
        run: python -m compileall -q src script tests

  verl-compat:
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface/hub
          key: hf-qwen25-05b-7ae557604adf67be50417f59c2c2f167def9a775
      - name: Install pinned CPU compatibility stack
        run: |
          python -m pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
          python -m pip install -r requirements/verl.txt
          python -m pip install "pytest>=8,<10"
          python -m pip install -e . --no-deps
      - name: Verify VeRL trainer import and Hydra entrypoint
        run: |
          python -c "import verl.trainer.sft_trainer"
          python -c "import verl.utils.dataset.rl_dataset"
          python -m verl.trainer.sft_trainer --help > /dev/null
      - name: Load exported SFT parquet through VeRL
        run: python -m pytest -q tests/sft/test_verl_compat.py
      - name: Load exported RL parquet through VeRL RLHFDataset
        run: python -m pytest -q tests/rl/test_rl_verl_compat.py
      - name: Drive choice reward through verL reward cfg loader (CPU reward loop)
        run: python -m pytest -q tests/rl/test_rl_verl_adapter_compat.py
